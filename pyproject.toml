[project]
name = "finbar"
version = "0.1.0"
description = "Financial bars microservice — raw OHLCV data + profiles via MCP + REST"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "fastmcp>=3.0.0",
    "yfinance>=0.2.50",
    "hyperliquid-python-sdk>=0.10.0",
    "sqlalchemy>=2.0.0",
    "pandas>=2.0.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0.0",
    "requests>=2.32.0",
    "pandas-ta>=0.3.14",
    "finbar-strategy-runtime[pandas,yaml]>=0.3.0,<0.4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.28.0",
    "black>=24.0.0",
    "ruff>=0.6.0",
]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.ruff.lint.per-file-ignores]
# ruff's isort (I001) destructively splits aliased multi-line imports
# (from X import foo as bar) in this handler registry file. The imports are
# correctly sorted; suppress the false positive.
"**/price_action.py" = ["I001"]

[tool.black]
line-length = 88
target-version = ["py312"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
asyncio_mode = "auto"
