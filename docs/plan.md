# Finbar — Implementation Plan

> **Financial Bars Microservice**: fetch raw OHLCV data + profiles from yfinance
> (later Hyperliquid), cache in SQLite, expose via REST API and MCP server.
> Strict clean architecture per AGENTS.md.
>
> **Scope**: raw OHLCV bars and symbol profiles only — no enrichment,
> no technical indicators, no Initial Balance, no trading metrics.

See the full plan in the conversation history or regenerate from the AGENTS.md
and the phase-by-phase implementation commits.
